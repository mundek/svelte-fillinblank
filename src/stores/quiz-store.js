import { readable, writable } from 'svelte/store';

export const sentence = writable('Vamos a pre*ver*lo con nuestros amigos *mañana*; nos interesa mucho.');
export const currentResponse = writable("");
